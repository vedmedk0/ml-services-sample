{
  // port for client/server
  port = 50051
  // sample is considered predicted if abs(score - label) < model-threshold
  model-threshold = 0.2
  // accuracy will be logged once per log-period samples
  log-period = 10
  // number of samples for generator
  input-data-count = 1000
}